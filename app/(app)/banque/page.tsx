import { ExerciseLibrary } from "@/components/banque/exercise-library";

// Banque d'exercices de renforcement. Contenu interactif (recherche/filtres)
// dans le composant client. Auth gérée par le layout du groupe (app).
export default function BanquePage() {
  return <ExerciseLibrary />;
}
