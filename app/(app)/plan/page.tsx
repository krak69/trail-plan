import { PlanView } from "@/components/plan/plan-view";

// Plan : assistant de création d'objectif (interactif) dans le composant client.
// Auth gérée par le layout du groupe (app).
export default function PlanPage() {
  return <PlanView />;
}
