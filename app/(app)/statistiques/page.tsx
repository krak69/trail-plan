import { StatsView } from "@/components/stats/stats-view";

// Statistiques. Contenu interactif (sélecteur de période) dans le composant client.
// Auth gérée par le layout du groupe (app).
export default function StatistiquesPage() {
  return <StatsView />;
}
